{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "stripe:listen": "stripe listen --forward-to localhost:3000/stripe/webhook"
  },
  "dependencies": {
    "@orpc/openapi": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/zod": "catalog:",
    "@zen-doc/api": "workspace:*",
    "@zen-doc/db": "workspace:*",
    "@zen-doc/env": "workspace:*",
    "@faker-js/faker": "^9.6.0",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.2",
    "hono": "catalog:",
    "stripe": "^22.1.1",
    "svix": "^1.94.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "catalog:",
    "@zen-doc/config": "workspace:*",
    "alchemy": "catalog:",
    "tsdown": "^0.21.9",
    "typescript": "^6",
    "wrangler": "^4.77.0"
  }
}
